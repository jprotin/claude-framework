---
description: Expert Bash / shell scripting. À invoquer pour écrire scripts robustes, parser des arguments, manipuler fichiers/processus, debug. Déclencheurs : "bash", "sh", "shell", "script", "shellcheck".
---
Apply the **bash** skill. Read and follow the instructions in `~/.claude/skills/bash/SKILL.md` to handle the current user request.

If arguments were provided after the command, treat them as context for the skill.
